version: '3.8'

services:
  goobereats-backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./cal_data:/app/cal_data
      - ./config.toml:/app/config.toml:ro
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
